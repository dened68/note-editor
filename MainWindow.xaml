<Window x:Class="redzam.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:redzam"
        mc:Ignorable="d"
        Title="MainWindow" Height="464.033" Width="345.322">
    <Grid>
        <ListBox x:Name="notelist" Panel.ZIndex="0"  Padding="0 0 0 0"  >
            <ListBox.ContextMenu>
                <ContextMenu/>
            </ListBox.ContextMenu>
            







        </ListBox>
        <Button x:Name="addButton" Click="addButton_Click" Width="50" Height="50" Panel.ZIndex="2" VerticalAlignment="Bottom" HorizontalAlignment="Right" >
            <Button.Template>
                <ControlTemplate  TargetType="Button">
                    <Grid>
                        <Ellipse Fill="#90D7C0" Opacity="0.3"></Ellipse>                       
                    </Grid>
                </ControlTemplate>
            </Button.Template>
        </Button>

    </Grid>
</Window>
